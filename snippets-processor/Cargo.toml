[package]
name = "mdbook-snippets"
version = "0.2.0"
edition = "2021"

# Prevent this from interfering with workspaces
[workspace]
members = ["."]

[dependencies]
clap = "4.5"
mdbook-preprocessor = "=0.5.4"
serde_json = "1.0"
